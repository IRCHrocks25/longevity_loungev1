// No longer used: we now pin Rollup to v3 via npm overrides in package.json,
// which avoids the native binary entirely. This file is kept only as a stub
// in case any old references remain.
console.log('install-rollup-native.js is deprecated; Rollup is pinned to v3 via overrides.')
